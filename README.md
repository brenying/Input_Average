# Input & Average  
*A Java program that collects five user-entered integers and outputs their total and average.*


## 📖 Overview
This program prompts the user to enter five integer values. After receiving all inputs, it calculates the total sum of the numbers and computes their average. The program then prints the entered values along with the computed results in a clean, readable format.


## 🚀 Features
- Accepts **five integer inputs** from the user  
- Displays all entered values  
- Computes:  
  - **Total sum**  
  - **Average value**  
- Simple and beginner-friendly Java structure  
- Uses `Scanner` for input handling  
